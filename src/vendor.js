/* refernce to vendor files and libraries
webpack can bundle these separately so
they don't get downloaded everytime the app code changes
*/
/* eslint-disable no-unused-vars */
import fetch from 'whatwg-fetch';
